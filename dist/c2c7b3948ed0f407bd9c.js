import*as THREE from"three";import*as CANNON from"cannon-es";import{FirstPersonControls}from"three/examples/jsm/controls/FirstPersonControls.js";import{OrbitControls}from"three/examples/jsm/controls/OrbitControls.js";const camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,.1,1e3);camera.position.set(0,-10,0);const topDownCamera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,.1,1e3);topDownCamera.position.set(0,20,0),topDownCamera.lookAt(0,0,0);let currentCamera=camera;document.addEventListener("keydown",(e=>{"c"===e.key&&(currentCamera=currentCamera===camera?topDownCamera:camera)}));export{camera,topDownCamera,currentCamera};