import*as THREE from"three";import*as CANNON from"cannon-es";import*as objects from"./objects.js";const directionalLight=new THREE.PointLight(16777215,1);directionalLight.position.set(0,23,0),objects.scene.add(directionalLight);const bulbGeometry=new THREE.SphereGeometry(5,16,16),bulbMaterial=new THREE.MeshStandardMaterial({emissive:16777198,emissiveIntensity:3});export const bulb=new THREE.Mesh(bulbGeometry,bulbMaterial);bulb.position.copy(directionalLight.position),objects.scene.add(bulb);